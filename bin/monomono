#!/bin/bash
# ==============================================================================
# MONOMONO - HAUPTSKRIPT (v11.2 - mit intelligentem App-Install-Check)
# ==============================================================================
# Dieses Skript nutzt eine private GitHub App für eine robuste und sichere
# Automatisierung und führt den Benutzer mit detaillierten Anweisungen.
# ==============================================================================

# --- Konfiguration & globale Variablen ---
CONFIG_DIR="$HOME/.monomono"
CONFIG_FILE="$CONFIG_DIR/config.json"

# --- Befehlslogik (Hilfe & Reset) ---
case "$1" in
    --help|-h)
        echo "MonoMono Assistent - Ein Werkzeug zum Erstellen von Fusions-Repos."
        echo
        echo "Verwendung:"
        echo "  monomono           - Startet den interaktiven Assistenten."
        echo "  monomono --reset   - Löscht die gespeicherte MonoMono-Konfiguration."
        echo "  monomono --help    - Zeigt diese Hilfe an."
        exit 0
        ;;
    --reset)
        if [ -d "$CONFIG_DIR" ]; then
            rm -rf "$CONFIG_DIR"
            echo "✔ MonoMono-Konfiguration vollständig zurückgesetzt."
        else
            echo "ℹ Es war keine Konfiguration zum Zurücksetzen vorhanden."
        fi
        exit 0
        ;;
esac


# --- Hilfsfunktionen ---
# Funktion, um sicherzustellen, dass die App konfiguriert ist
ensure_app_configured() {
    if [ -f "$CONFIG_FILE" ] && \
       jq -e '.app_id' "$CONFIG_FILE" >/dev/null 2>&1 && \
       jq -e '.private_key_path' "$CONFIG_FILE" >/dev/null 2>&1 && \
       jq -e '.app_name' "$CONFIG_FILE" >/dev/null 2>&1; then
        echo "✔ MonoMono App-Konfiguration gefunden."
        return 0
    fi

    # --- APP-SETUP-WIZARD ---
    echo "--------------------------------------------------------------------"
    echo "🔑 INFO: EINMALIGES APP-SETUP NÖTIG"
    echo "MonoMono benötigt eine private 'Bot'-App für eine sichere & stabile Verbindung."
    echo "Das ist eine einmalige Einrichtung, die ca. 3 Minuten dauert."
    echo "--------------------------------------------------------------------"
    echo "‼️  AKTION ERFORDERLICH: Bitte öffne diese URL in deinem lokalen Browser:"
    echo "    https://github.com/settings/apps/new"
    
    echo
    echo "--- ANLEITUNG IM BROWSER ---"
    echo "1. App name:         Gib einen eindeutigen Namen ein (z.B. 'Meine-MonoMono-App')"
    echo "2. Homepage URL:     https://github.com/mannomannX/MonoMono"
    echo "3. Webhook:          ENTFERNE den Haken bei 'Active'. Das Feld muss leer sein."
    echo "4. Permissions -> Repository permissions:"
    echo "   - Actions:       Read & write"
    echo "   - Contents:      Read & write"
    echo "   - Administration:Read & write (für Secrets & Webhooks)"
    echo "5. Klicke ganz unten auf 'Create GitHub App'."
    echo "-------------------------------------------"
    
    read -p "? Drücke ENTER, nachdem du die App erstellt hast..."

    echo
    echo "--- DATEN SAMMELN ---"
    echo "Du befindest dich nun auf der Einstellungsseite deiner neuen App."
    echo
    echo "1. App-Namen eingeben:"
    echo "   - Der Name, den du gerade für deine App gewählt hast."
    read -p "? Bitte gib den exakten 'App name' hier ein: " NEW_APP_NAME

    echo
    echo "2. App ID finden und kopieren:"
    echo "   - Suche oben auf der Seite nach der 'App ID'. Es ist eine mehrstellige Zahl."
    read -p "? Bitte gib deine 'App ID' hier ein: " NEW_APP_ID
    
    echo
    echo "3. Privaten Schlüssel herunterladen:"
    echo "   - Scrolle auf der Seite nach unten zum Abschnitt 'Private keys'."
    echo "   - Klicke auf den Button [Generate a private key]."
    echo "   - Dein Browser wird jetzt eine Datei mit der Endung .pem herunterladen."
    echo "     (Meistens landet sie in deinem 'Downloads'-Ordner)."
    echo
    echo "4. Pfad zur Datei angeben:"
    echo "   - Beispiel für Windows: C:\Users\DeinName\Downloads\deine-app.private-key.pem"
    echo "   - Beispiel für macOS:   /Users/DeinName/Downloads/deine-app.private-key.pem"
    read -p "? Bitte gib den vollständigen Pfad zu dieser .pem-Datei ein: " NEW_PRIVATE_KEY_PATH

    mkdir -p "$CONFIG_DIR"
    echo "{\"app_id\":\"$NEW_APP_ID\",\"private_key_path\":\"$NEW_PRIVATE_KEY_PATH\",\"app_name\":\"$NEW_APP_NAME\"}" > "$CONFIG_FILE"
    
    echo
    echo "✅ App-Konfiguration erfolgreich gespeichert in $CONFIG_FILE!"
    echo "--------------------------------------------------------------------"
}

# Funktion zur Authentifizierung als App und zum Abrufen eines Installationstokens
get_installation_token() {
    APP_ID=$(jq -r '.app_id' "$CONFIG_FILE")
    PRIVATE_KEY_PATH=$(jq -r '.private_key_path' "$CONFIG_FILE")
    GITHUB_USER=$(jq -r '.github_user' "$CONFIG_FILE")

    # Erstelle Header und Payload für das JWT
    NOW=$(date +%s)
    IAT=$(($NOW - 60))
    EXP=$(($NOW + 600))
    HEADER_B64=$(echo -n '{"alg":"RS256","typ":"JWT"}' | base64 | tr -d '\n' | tr -d '=' | tr '/+' '_-')
    PAYLOAD_B64=$(echo -n "{\"iat\":$IAT,\"exp\":$EXP,\"iss\":\"$APP_ID\"}" | base64 | tr -d '\n' | tr -d '=' | tr '/+' '_-')

    # Signiere das JWT mit dem privaten Schlüssel
    SIGNATURE_B64=$(openssl dgst -sha256 -sign "$PRIVATE_KEY_PATH" <(echo -n "$HEADER_B64.$PAYLOAD_B64") | base64 | tr -d '\n' | tr -d '=' | tr '/+' '_-')
    JWT="$HEADER_B64.$PAYLOAD_B64.$SIGNATURE_B64"

    # Finde die Installations-ID für den Benutzer-Account
    INSTALLATION_ID=$(curl -s -H "Authorization: Bearer $JWT" -H "Accept: application/vnd.github.v3+json" https://api.github.com/app/installations | jq ".[] | select(.account.login==\"$GITHUB_USER\") | .id")

    if [ -z "$INSTALLATION_ID" ]; then
        echo "❌ Fehler: Die App ist nicht für deinen Account '$GITHUB_USER' installiert."
        echo "   Gehe zu https://github.com/settings/apps, wähle deine App aus und installiere sie."
        exit 1
    fi

    # Erstelle einen kurzlebigen Installation-Token
    INSTALLATION_TOKEN=$(curl -s -X POST -H "Authorization: Bearer $JWT" -H "Accept: application/vnd.github.v3+json" "https://api.github.com/app/installations/$INSTALLATION_ID/access_tokens" | jq -r '.token')
    
    if [ -z "$INSTALLATION_TOKEN" ]; then
        echo "❌ Fehler beim Erstellen des Installation-Tokens."
        exit 1
    fi

    export GH_TOKEN=$INSTALLATION_TOKEN
    echo "✔ Erfolgreich als MonoMono-App authentifiziert."
}

# --- HAUPTLOGIK ---

# 1. PRÜFUNG DER VORAUSSETZUNGEN
if ! command -v gh &>/dev/null || ! command -v fzf &>/dev/null || ! command -v jq &>/dev/null; then
    # --- SETUP-MODUS ---
    echo "🔎 Checking system requirements..."
    echo "--------------------------------------------------------------------"
    echo "⚠️  One or more required tools are not available."
    echo "   Starting the one-time installation wizard..."
    echo "--------------------------------------------------------------------"
    OS_TYPE="$(uname -s)"
    read -p "? Required tools (gh, fzf, jq) are missing. Install them now? (y/n) " choice
    if [[ ! "$choice" =~ ^[YyJj]$ ]]; then echo "❌ Aborted by user."; exit 1; fi

    case "$OS_TYPE" in
        Linux*)
            if command -v apt-get &>/dev/null; then
                echo "-> Updating package lists (apt-get update)..."
                sudo apt-get update
                if ! command -v gh &>/dev/null; then echo "-> Installing 'GitHub CLI' (gh)..."; sudo apt-get install -y gh; fi
                if ! command -v fzf &>/dev/null; then echo "-> Installing 'fzf'..."; sudo apt-get install -y fzf; fi
                if ! command -v jq &>/dev/null; then echo "-> Installing 'jq'..."; sudo apt-get install -y jq; fi
            elif command -v dnf &>/dev/null; then
                if ! command -v gh &>/dev/null; then sudo dnf install -y gh; fi
                if ! command -v fzf &>/dev/null; then sudo dnf install -y fzf; fi
                if ! command -v jq &>/dev/null; then sudo dnf install -y jq; fi
            else
                echo "❌ Error: Could not find a suitable Linux package manager (apt, dnf)." >&2; exit 1
            fi
            ;;
        Darwin*)
            if ! command -v brew &>/dev/null; then echo "❌ Error: Homebrew (brew) not found."; exit 1; fi
            if ! command -v gh &>/dev/null; then brew install gh; fi
            if ! command -v fzf &>/dev/null; then brew install fzf; fi
            if ! command -v jq &>/dev/null; then brew install jq; fi
            ;;
        *) echo "❌ Error: Unsupported operating system ($OS_TYPE)." >&2; exit 1;;
    esac
    echo
    echo "--------------------------------------------------------------------"
    echo "✅ SETUP COMPLETE. Please close this terminal and open a new one."
    echo "--------------------------------------------------------------------"
    exit 0
fi

# --- ARBEITS-MODUS ---
echo "✔ System ready. All tools found."

# 1. App-Konfiguration sicherstellen
ensure_app_configured

# 2. GITHUB-BENUTZERNAME LADEN/SPEICHERN
if [ -f "$CONFIG_FILE" ] && jq -e '.github_user' "$CONFIG_FILE" >/dev/null 2>&1; then
    GITHUB_USER=$(jq -r '.github_user' "$CONFIG_FILE")
else
    read -p "? Bitte gib deinen GitHub-Benutzernamen ein: " GITHUB_USER
    jq --arg user "$GITHUB_USER" '.github_user = $user' "$CONFIG_FILE" > tmp.$$.json && mv tmp.$$.json "$CONFIG_FILE"
fi
echo "ℹ Angemeldet als Benutzer: $GITHUB_USER"

# 3. Als App authentifizieren
get_installation_token

# 4. PROJEKT-SETUP
echo "-------------------------------------------"
echo "🚀 Starte den Assistenten..."
echo "1/5: Lade Repositories via API..."
REPOS_LIST=$(curl -s -H "Authorization: token $PAT" "https://api.github.com/user/repos?type=owner&per_page=100" | jq -r '.[].full_name')
if [ -z "$REPOS_LIST" ]; then echo "❌ Keine Repositories gefunden."; exit 1; fi

echo -e "2/5: Wähle die zu fusionierenden Repos:\n(Navigation: Pfeiltasten, Auswahl: TAB, Bestätigen: ENTER)"
SELECTED_REPOS=$(echo "$REPOS_LIST" | fzf --multi --height 40% --border --prompt="Select> ")
if [ -z "$SELECTED_REPOS" ]; then echo "❌ Aborted: No repositories selected."; exit 1; fi
SUB_REPOS=$(echo "$SELECTED_REPOS" | tr '\n' ',' | sed 's/,$//')

while true; do
    read -p "? 3/5: Wie soll das neue Fusions-Repo heißen? (Keine Leerzeichen): " FUSION_REPO_NAME
    if [[ "$FUSION_REPO_NAME" =~ \  ]]; then
        echo "❌ Fehler: Der Name darf keine Leerzeichen enthalten."
    elif [ -z "$FUSION_REPO_NAME" ]; then
        echo "❌ Fehler: Der Name darf nicht leer sein. Bitte versuche es erneut."
    else
        break
    fi
done

VISIBILITY_FLAG="--public"
read -p "? 4/5: Soll das Repository 'privat' sein? (j/n) " IS_PRIVATE
if [[ "$IS_PRIVATE" =~ ^[YyJj]$ ]]; then
    VISIBILITY_FLAG="--private"
fi

# 5. UPDATE-OPTIONEN
echo "-------------------------------------------"
echo "⚙️ 5/5: Konfiguriere die Update-Methoden..."
CRON_SCHEDULE=""
read -p "? Möchtest du einen automatischen, zeitgesteuerten Sync aktivieren? (j/n) " ACTIVATE_SCHEDULE
if [[ "$ACTIVATE_SCHEDULE" =~ ^[YyJj]$ ]]; then
    read -p "? In welchem Intervall (in Stunden) soll der Sync laufen? (1-24) " SCHEDULE_HOURS
    CRON_SCHEDULE="  schedule:\n    - cron: '0 */${SCHEDULE_HOURS:-24} * * *'"
fi

SETUP_WEBHOOKS=false
read -p "? Sollen Echtzeit-Updates über Sub-Repo-Workflows eingerichtet werden? (j/n) " ACTIVATE_WEBHOOKS
if [[ "$ACTIVATE_WEBHOOKS" =~ ^[YyJj]$ ]]; then
    SETUP_WEBHOOKS=true
fi

# 6. REPO ERSTELLEN & WORKFLOW PUSHEN
echo "-> Erstelle Repo '$FUSION_REPO_NAME' und richte den Workflow ein..."
gh repo create "$GITHUB_USER/$FUSION_REPO_NAME" "$VISIBILITY_FLAG" --description "Automatic fusion repo by MonoMono" >/dev/null || { echo "❌ Error creating repo."; exit 1; }
echo -n "$PAT" | gh secret set ACCESS_TOKEN --repo "$GITHUB_USER/$FUSION_REPO_NAME" || { echo "❌ Fehler beim Setzen des Secrets."; exit 1; }
echo "✔ Secret erfolgreich im Repository gespeichert."

WORKFLOW_NAME="Fusion-Repo Erstellen & Aktualisieren"
ON_TRIGGERS="on:\n  workflow_dispatch:"
if [ -n "$CRON_SCHEDULE" ]; then
    ON_TRIGGERS+="\n$CRON_SCHEDULE"
fi

read -r -d '' WORKFLOW_CONTENT << EOM
name: 'Fusion-Repo Erstellen & Aktualisieren'
on:
  workflow_dispatch:
  repository_dispatch:
    types: [monomono-sync-event]
jobs:
  sync-repos:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout fusion repo
        uses: actions/checkout@v4
        with:
          token: \${{ secrets.ACCESS_TOKEN }}
      - name: Clone sub-repos
        run: |
          git config --global url."https://x-access-token:\${{ secrets.ACCESS_TOKEN }}@github.com/".insteadOf "https://github.com/"
          REPOS_TO_SYNC=\${{ inputs.repos || '$SUB_REPOS' }}
          for repo in \$(echo "\$REPOS_TO_SYNC" | sed 's/,/ /g'); do
            folder_name=\$(basename \$repo)
            echo "-> Cloning \$repo into \$folder_name..."
            rm -rf "\$folder_name"
            git clone --depth 1 https://github.com/\$repo.git "\$folder_name" && rm -rf "\$folder_name/.git"
          done
      - name: Create intelligent README
        run: |
          sudo apt-get update >/dev/null && sudo apt-get install -y tree >/dev/null
          echo "# 🤖 MonoMono Fusions-Repo" > README.md
          echo "" >> README.md
          echo "Dieses Repository ist eine automatisch generierte Zusammenführung." >> README.md
          echo "" >> README.md
          echo "## 🗺️ Projekt-Map" >> README.md
          echo "### 🧩 Projekt-Komponenten" >> README.md
          for repo in \$(echo "\${{ inputs.repos || '$SUB_REPOS' }}" | sed 's/,/ /g'); do
            echo "- **[\$(basename \$repo)](./\$(basename \$repo))** (Original: [\$repo](https://github.com/\$repo))" >> README.md
          done
          echo "" >> README.md
          echo "### 🌳 Verzeichnisstruktur" >> README.md
          echo "\`\`\`" >> README.md
          tree -L 2 -I 'README.md' >> README.md
          echo "\`\`\`" >> README.md
          echo "" >> README.md
          echo "> Letzte Aktualisierung: \$(date)" >> README.md
      - name: Commit and push changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add .
          if ! git diff-index --quiet HEAD; then
            git commit -m "feat: Sync sub-repos and update project map"
            git push
          else
            echo "No new changes found."
          fi
EOM
TEMP_DIR=$(mktemp -d)
cd "$TEMP_DIR" || exit 1
git clone "https://x-access-token:$PAT@github.com/$GITHUB_USER/$FUSION_REPO_NAME.git" . >/dev/null 2>&1 || { echo "❌ Error cloning repo."; rm -rf "$TEMP_DIR"; exit 1; }
mkdir -p .github/workflows
echo -e "$WORKFLOW_CONTENT" > .github/workflows/sync.yml
git config user.name "MonoMono Script"
git config user.email "bot@users.noreply.github.com"
git add .
git commit -m "CI: Add initial sync workflow"
git push --force origin main >/dev/null 2>&1 || { echo "❌ Error pushing workflow."; rm -rf "$TEMP_DIR"; exit 1; }
cd - >/dev/null
rm -rf "$TEMP_DIR"
echo "✔ Workflow file successfully pushed to GitHub."

# 7. ECHTZEIT-SYNC EINRICHTEN (TRIGGER-WORKFLOWS)
if [[ "$SETUP_WEBHOOKS" = true ]]; then
    echo "-> Richte Echtzeit-Synchronisierung für die Sub-Repos ein..."

    # ==============================================================================
    # ✨ HIER IST DER FINALE FIX ✨
    # Wir übergeben den PAT-Secret korrekt an die GH_TOKEN-Umgebungsvariable.
    # ==============================================================================
    read -r -d '' TRIGGER_WORKFLOW_CONTENT << EOM
# MonoMono Trigger Workflow
name: MonoMono Sync Trigger
on:
  push:
    branches: [main, master]
jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Send dispatch to fusion repo
        env:
          FUSION_REPO: \${{ secrets.MONOMONO_FUSION_REPO }}
          GH_TOKEN: \${{ secrets.MONOMONO_PAT }}
        run: |
          if [ -z "\$FUSION_REPO" ]; then
            echo "Fehler: Secret MONOMONO_FUSION_REPO ist nicht gesetzt."
            exit 1
          fi
          
          echo "Sending dispatch to \$FUSION_REPO"
          gh api --method POST "repos/\$FUSION_REPO/dispatches" -f "event_type=monomono-sync-event"
EOM

    for repo in $(echo $SUB_REPOS | sed "s/,/ /g"); do
        echo "   - Konfiguriere Sub-Repo: $repo"
        
        FUSION_REPO_FULL_NAME="$GITHUB_USER/$FUSION_REPO_NAME"
        echo "     - Setze Secrets im Sub-Repo..."
        echo -n "$FUSION_REPO_FULL_NAME" | gh secret set MONOMONO_FUSION_REPO --repo "$repo"
        echo -n "$PAT" | gh secret set MONOMONO_PAT --repo "$repo"

        echo "     - Pushe Trigger-Workflow zum Sub-Repo..."
        TEMP_SUB_DIR=$(mktemp -d)
        git clone "https://x-access-token:$PAT@github.com/$repo.git" "$TEMP_SUB_DIR" >/dev/null 2>&1
        
        mkdir -p "$TEMP_SUB_DIR/.github/workflows"
        echo -e "$TRIGGER_WORKFLOW_CONTENT" > "$TEMP_SUB_DIR/.github/workflows/monomono-trigger.yml"
        
        cd "$TEMP_SUB_DIR" || exit
        git config user.name "MonoMono Script"
        git add .github/workflows/monomono-trigger.yml
        if ! git diff-index --quiet HEAD; then
            git commit -m "feat: Add MonoMono sync trigger"
            git push
        else
            echo "     - Trigger-Workflow ist bereits aktuell."
        fi
        cd - >/dev/null
        rm -rf "$TEMP_SUB_DIR"
    done
fi

# 8. WORKFLOW STARTEN
echo "-> Warte, bis der Workflow auf GitHub sichtbar ist..."
WORKFLOW_FOUND=false
for i in {1..5}; do
  if gh workflow list --repo "$GITHUB_USER/$FUSION_REPO_NAME" 2>/dev/null | grep -q "$WORKFLOW_NAME"; then
    echo "✔ Workflow gefunden."; WORKFLOW_FOUND=true; break;
  fi
  if [ $i -lt 5 ]; then echo "   - Noch nicht gefunden, versuche es in 2 Sekunden erneut..."; sleep 2; fi
done
if ! $WORKFLOW_FOUND; then echo "❌ Error: Workflow not found after 10 seconds."; exit 1; fi
echo "-> Starte den ersten Lauf des Workflows..."
gh workflow run sync.yml --repo "$GITHUB_USER/$FUSION_REPO_NAME" || { echo "❌ Error starting the workflow."; exit 1; }

echo
echo "$TEXT_ALL_DONE"
echo "👀 Verfolge den Fortschritt hier: https://github.com/$GITHUB_USER/$FUSION_REPO_NAME/actions"