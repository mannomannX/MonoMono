#!/bin/bash
# ==============================================================================
# MONOMONO-CONNECT (v1.0)
# ==============================================================================
# Verbindet ein bestehendes Repository mit dem MonoMono-System, indem es den
# Update-Workflow installiert und die nötigen Verbindungen (Secrets, Webhooks)
# für die Synchronisierung einrichtet.
# ==============================================================================

# Hilfsfunktion, um eine URL im Standardbrowser zu öffnen.
open_url() {
  local url=$1
  case "$(uname -s)" in
    Linux*)   xdg-open "$url" >/dev/null 2>&1 ;;
    Darwin*)  open "$url" ;;
    CYGWIN*|MINGW*|MSYS*) # Git Bash on Windows
              start "" "$url" ;;
    *)        echo "Konnte den Browser nicht automatisch öffnen."
              echo "Bitte öffne diese URL manuell: $url" ;;
  esac
}

# --- HAUPTLOGIK ---

echo "MonoMono Connect Assistent"
echo "---------------------------"
echo "Dieses Skript installiert den Update-Workflow in einem bestehenden Repository."
echo

# 1. SETUP-PRÜFUNG (vereinfacht, da die Tools für den Hauptbefehl schon da sein sollten)
if ! command -v gh &>/dev/null || ! command -v fzf &>/dev/null; then
    echo "❌ Fehler: Die Kern-Abhängigkeiten 'gh' und 'fzf' sind nicht installiert."
    echo "   Bitte führe zuerst das Hauptskript 'monomono' aus, um das Setup durchzuführen."
    exit 1
fi

# 2. GITHUB-LOGIN-PRÜFUNG
if ! gh auth status &>/dev/null; then
  echo "🔑 Du bist nicht bei der GitHub CLI angemeldet. Bitte melde dich zuerst an mit: gh auth login"
  exit 1
fi
echo "✔ Erfolgreich bei GitHub angemeldet."

# 3. BENUTZER-EINGABEN
# Lädt den gespeicherten Benutzernamen oder fragt danach
CONFIG_FILE="$HOME/.fusion_assistant_config"
if [ -f "$CONFIG_FILE" ]; then
    source "$CONFIG_FILE"
    echo "ℹ Gespeicherten Benutzernamen gefunden: $GITHUB_USER"
else
    read -p "? Bitte gib deinen GitHub-Benutzernamen ein: " GITHUB_USER
    if [ -z "$GITHUB_USER" ]; then echo "❌ Name darf nicht leer sein."; exit 1; fi
    echo "GITHUB_USER=\"$GITHUB_USER\"" > "$CONFIG_FILE"
    echo "✔ Dein Benutzername wurde für die nächste Ausführung gespeichert."
fi

read -p "? Welches bestehende Repo soll verbunden werden? (z.B. $GITHUB_USER/mein-repo): " FUSION_REPO
if [ -z "$FUSION_REPO" ]; then echo "❌ Fehler: Eingabe darf nicht leer sein."; exit 1; fi

echo "-> Lade verfügbare Repositories für die Auswahl..."
REPOS_LIST=$(gh repo list "$GITHUB_USER" --limit 1000 --json "nameWithOwner" --jq '.[].nameWithOwner')
if [ -z "$REPOS_LIST" ]; then echo "❌ Keine Repositories für '$GITHUB_USER' gefunden."; exit 1; fi

echo -e "? Welche Sub-Repos sollen damit verbunden werden?\n(Navigation: Pfeiltasten, Auswahl: TAB, Bestätigen: ENTER)"
SELECTED_REPOS=$(echo "$REPOS_LIST" | fzf --multi --height 40% --border --prompt="Auswahl> ")
if [ -z "$SELECTED_REPOS" ]; then echo "❌ Abbruch: Keine Repositories ausgewählt."; exit 1; fi
SUB_REPOS=$(echo "$SELECTED_REPOS" | tr '\n' ',' | sed 's/,$//')

# 4. UPDATE-OPTIONEN
echo "-------------------------------------------"
echo "⚙️ Konfiguriere die Update-Methoden für '$FUSION_REPO'..."
CRON_SCHEDULE=""
read -p "? Möchtest du einen automatischen, zeitgesteuerten Sync aktivieren? (j/n) " ACTIVATE_SCHEDULE
if [[ "$ACTIVATE_SCHEDULE" == "j" || "$ACTIVATE_SCHEDULE" == "J" ]]; then
    read -p "? In welchem Intervall (in Stunden) soll der Sync laufen? (1-24) " SCHEDULE_HOURS
    CRON_SCHEDULE="  schedule:\n    - cron: '0 */${SCHEDULE_HOURS:-24} * * *'"
fi

SETUP_WEBHOOKS=false
read -p "? Sollen Webhooks für Echtzeit-Updates bei jedem Push eingerichtet werden? (j/n) " ACTIVATE_WEBHOOKS
if [[ "$ACTIVATE_WEBHOOKS" == "j" || "$ACTIVATE_WEBHOOKS" == "J" ]]; then
    SETUP_WEBHOOKS=true
fi


# 5. DELUXE AUTHENTIFIZIERUNGS-WORKFLOW
echo
echo "--------------------------------------------------------------------"
echo "🔑 INFO: ZUGRIFFS-TOKEN BENÖTIGT"
read -p "? Drücke ENTER, um die Token-Erstellung im Browser zu öffnen..."

open_url "https://github.com/settings/tokens/new"
echo
echo "‼️ ANLEITUNG IM BROWSER:"
echo "   1. Note: Gib dem Token einen Namen (z.B. 'MonoMono_Connect_Token')."
echo "   2. Scopes: Wähle den Haken bei 'repo'."
echo "   3. Klicke ganz unten auf 'Generate token' und kopiere den Token."
echo "--------------------------------------------------------------------"

PAT=""
while [ -z "$PAT" ]; do
    read -s -p "👉 Bitte füge den kopierten Token hier ein und drücke ENTER: " PAT
    echo
done
echo "-> Speichere den Token sicher als 'ACCESS_TOKEN' Secret im Repo..."
echo -n "$PAT" | gh secret set ACCESS_TOKEN --repo "$FUSION_REPO" || { echo "❌ Fehler beim Setzen des Secrets."; exit 1; }
echo "✔ Secret erfolgreich im Repository '$FUSION_REPO' gespeichert."


# 6. WORKFLOW ERSTELLEN UND PUSHEN
WORKFLOW_NAME="Fusion-Repo Erstellen & Aktualisieren"
ON_TRIGGERS="on:\n  workflow_dispatch:\n"
if [ -n "$CRON_SCHEDULE" ]; then ON_TRIGGERS+="$CRON_SCHEDULE\n"; fi
if $SETUP_WEBHOOKS; then ON_TRIGGERS+="  repository_dispatch:\n    types: [monomono-sync-event]\n"; fi

read -r -d '' WORKFLOW_CONTENT << EOM
name: '$WORKFLOW_NAME'
$ON_TRIGGERS
jobs:
  sync-repos:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Fusions-Repo auschecken
        uses: actions/checkout@v4
      - name: Sub-Repos klonen
        run: |
          REPOS_TO_SYNC='$SUB_REPOS'
          for repo in \$(echo "\$REPOS_TO_SYNC" | sed 's/,/ /g'); do
            folder_name=\$(basename \$repo)
            echo "-> Klone \$repo in den Ordner \$folder_name..."
            git clone --depth 1 https://\${{ secrets.ACCESS_TOKEN }}@github.com/\$repo.git \$folder_name && rm -rf \$folder_name/.git
          done
      - name: README erstellen
        run: |
          REPOS_TO_SYNC='$SUB_REPOS'
          echo "# Fusion-Repo" > README.md
          echo "" >> README.md
          echo "Dieses Repository ist ein automatischer Zusammenschluss der folgenden Projekte:" >> README.md
          for repo in \$(echo "\$REPOS_TO_SYNC" | sed 's/,/ /g'); do
            echo "- [\$repo](https://github.com/\$repo)" >> README.md
          done
          echo "" >> README.md
          echo "Letzte Aktualisierung: \$(date)" >> README.md
      - name: Änderungen committen & pushen
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add .
          if ! git diff-index --quiet HEAD; then
            git commit -m "Fusion-Repo: Sub-Repos synchronisiert"
            git pull --rebase
            git push
          else
            echo "Keine neuen Änderungen in den Sub-Repos gefunden."
          fi
EOM

echo "-> Richte temporären lokalen Klon ein, um den Workflow zu pushen..."
TEMP_DIR=$(mktemp -d)
cd "$TEMP_DIR" || exit 1
gh repo clone "$FUSION_REPO" . >/dev/null 2>&1 || { echo "❌ Fehler beim Klonen des Ziel-Repos."; rm -rf "$TEMP_DIR"; exit 1; }
mkdir -p .github/workflows
echo "$WORKFLOW_CONTENT" > .github/workflows/sync.yml
git config user.name "MonoMono Connect"
git config user.email "bot@users.noreply.github.com"
git add .
git commit -m "CI: Add/Update MonoMono sync workflow"
git push || { echo "❌ Fehler beim Pushen des Workflows."; rm -rf "$TEMP_DIR"; exit 1; }
cd - >/dev/null
rm -rf "$TEMP_DIR"
echo "✔ Workflow-Datei erfolgreich nach '$FUSION_REPO' gepusht."


# 7. WEBHOOKS EINRICHTEN
if $SETUP_WEBHOOKS; then
    echo "-> Richte Webhooks für die ausgewählten Sub-Repos ein..."
    DISPATCH_URL="https://api.github.com/repos/$FUSION_REPO/dispatches"
    
    for repo in $(echo $SUB_REPOS | sed "s/,/ /g"); do
        echo "   - Für Repo: $repo"
        gh api --method POST "repos/$repo/hooks" \
           -f "name=web" \
           -f "active=true" \
           -f "events[]=push" \
           -f "config[url]=$DISPATCH_URL" \
           -f "config[content_type]=json" \
           -F "event_type=monomono-sync-event" \
           --silent || echo "     ❌ Fehler beim Einrichten des Webhooks für $repo."
    done
    echo "✔ Webhooks eingerichtet."
fi

# 8. WORKFLOW STARTEN
echo "-> Starte den ersten Lauf des Workflows..."
gh workflow run sync.yml --repo "$FUSION_REPO" || { echo "❌ Fehler beim Starten des Workflows."; exit 1; }

echo
echo "✅ Verbindung für '$FUSION_REPO' erfolgreich hergestellt."
echo "   Der erste Synchronisations-Lauf wurde gestartet."
echo "👀 Verfolge den Fortschritt hier: https://github.com/$FUSION_REPO/actions"